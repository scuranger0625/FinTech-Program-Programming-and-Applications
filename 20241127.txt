numpy的最基礎物件:  ndarray (regular)、 ndarray (record)


Python 提供了一些非常有用且靈活的通用數據結構：例如列表（list）對象，它們具有許多方便的特性和應用領域。

列表對象可以被視為真正的工作碼：它們具有許多方便的特性和應用領域。

使用這種靈活（可變）的資料結構是有代價的：例如相對較高的內存使用和較慢的性能。

數組是這方面最重要的資料結構之一：數組通常將其他（基本）對象結構化為相同數據類型的行和列。


實際在業界較常使用array 而不是使用python的list

基本上陣列以二維陣列為主 除了訓練資料之外 較少使用三維以上的陣列

pandas套件的底層是numpy 但較常使用pandas套件

python的彈性很大 可以把不同維度的資料都塞進去 把任何的object放入list

重要: array必須是相同的資料型態 list的資料型態若是不盡相同 則無法直接轉換成array

